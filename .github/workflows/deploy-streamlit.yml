name: Deploy to Streamlit Cloud

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Deploy info
        run: |
          echo "Repository ready for Streamlit Cloud deployment"
          echo "Main file: app/main.py"
          echo "Configure at https://share.streamlit.io"
