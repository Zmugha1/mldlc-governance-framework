# Data Contract Policy
# Schema validation, missingness, PII

max_missingness_pct: 0.05
pii_allowed: false
schema_registry_required: true

quality_thresholds:
  min_rows: 100
  max_duplicate_pct: 0.01
