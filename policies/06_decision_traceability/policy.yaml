# Decision Traceability Policy
# Prediction audit, run manifest, lineage

prediction_audit_required: true
run_manifest_required: true
lineage_log_required: true

artifact_requirements:
  - explainability_bundle.json
  - run_manifest.json
  - artifacts_index.json
  - lineage.jsonl

all_artifacts_must_be_hashed: true
