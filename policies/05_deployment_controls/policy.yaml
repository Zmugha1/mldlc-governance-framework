# Deployment Controls Policy
# Drift, retraining, rollback

drift_policy_required: true
retraining_triggers:
  - accuracy_drop_below_threshold
  - drift_score_above_threshold
  - scheduled_interval_days

rollback_protocol_required: true
monitoring:
  real_time_drift_detection: true
  alert_threshold_pct: 0.90
